version: '3'

services:
    de4aconnector:
        image: de4a/connector:latest
        ports:
            - "8080:8080"
        volumes:
            - ./config/application.properties:/usr/local/tomcat/webapps/ROOT/WEB-INF/classes/application.properties
            - ./config/application.yml:/usr/local/tomcat/webapps/ROOT/WEB-INF/classes/application.yml
            - /tmp/connector:/tmp
